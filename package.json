{
  "name": "sentrypeer-restful-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/openapi-cli": "^1.0.0-beta.73"
  },
  "private": true,
  "scripts": {
    "start": "openapi --port 8083 preview-docs",
    "build": "openapi bundle -o dist",
    "test": "openapi lint"
  }
}
